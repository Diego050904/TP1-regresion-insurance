# ---- Entorno virtual ----
.venv/
venv/
env/

# ---- Python ----
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ipynb_checkpoints/

# ---- Jupyter ----
.jupyter/
*.nbconvert.ipynb

# ---- Datos derivados ----
# El CSV original (data/raw/) SI se versiona: es la fuente de verdad del TP.
# Los splits y datasets procesados NO: se regeneran de forma determinista
# ejecutando el codigo con la semilla fijada en src/config.py.
data/processed/*
!data/processed/.gitkeep

# ---- Salidas regenerables ----
# Las figuras se regeneran desde los notebooks. Si alguna la usas en la
# presentacion y no queres depender de re-ejecutar, forzala con: git add -f
reports/figures/*
!reports/figures/.gitkeep

# ---- Presentacion ----
# Se entrega por separado; no forma parte del repositorio.
*.pptx
~$*.pptx

# ---- Respaldo local de documentacion ----
# Copia de los docstrings extendidos que estaban en src/ antes de simplificarlos.
# Es material de consulta personal, no forma parte de la entrega.
documentacion_codigo.txt

# ---- Sistema operativo / editor ----
.DS_Store
Thumbs.db
desktop.ini
.vscode/*
!.vscode/settings.json
.vs/
